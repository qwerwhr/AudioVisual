# 2026-06-18 工作日志

## AudioVisual 功能升级（综合）
完成了以下8项功能升级：

1. **可可影视加入**：DEFAULT_DRAMA_SITES 和 main.js dramaSites 均添加 keke6.app
2. **网址自动更新机制**：main.js 新增 SITE_DOMAIN_MAP、checkSiteAvailability、searchAndUpdateSiteUrl 函数和 IPC 通道
3. **优酷主页显示**：移除 youkuCustomPage 显示逻辑，优酷直接导航到主页
4. **优酷反爬虫绕过**：preload-web.js 注入反检测脚本（navigator.webdriver/languages/plugins/chrome 覆写），main.js did-start-navigation 时额外注入
5. **接口搜索功能**：renderer.js 新增 searchAndFetchApiList 函数，搜索按钮触发
6. **分辨率分组**：DEFAULT_API_LIST 添加 resolution 字段，refreshDynamicUI 按 optgroup 分组显示，分辨率筛选下拉框
7. **失效接口弃用**：renderer.js 新增 checkAllApiAvailability，标记 deprecated，SettingsManager 支持新字段
8. **弹幕保留**：移除 bilibili-player-video-wrap 等元素的 CSS 屏蔽，iframe z-index 从 2147483647 改为 2147483646

额外改进：
- 优酷/哔哩哔哩视频页面加入自动解析触发（did-navigate、preload-web、renderer.js）
- preload-ui.js 新增 IPC 桥接（checkAndUpdateSite、checkAllSites、onSiteUrlUpdated、onSitesCheckResults）
- 影视导航上限从4改为5

所有JS文件语法检查通过。
